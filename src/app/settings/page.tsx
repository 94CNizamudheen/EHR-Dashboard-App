"use client";
export default function SettingsPage() {
  return (
    <div>
      <h1>Settings</h1>
      <p>Environment variables for API config are set in <code>.env.local</code>.</p>
    </div>
  );
}
